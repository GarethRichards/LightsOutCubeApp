<UserControl x:Class="LightsOutCube.Controls.CelebrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:LightsOutCube.Controls"
             xmlns:vm="clr-namespace:LightsOutCube.ViewModels"
             >
    <Border x:Name="Root" Opacity="0.75"
            HorizontalAlignment="Center" VerticalAlignment="Top" Margin="12,12,12,12" CornerRadius="8" Padding="12" Width="450" Height="450">
        <Border.Background>
            <StaticResource ResourceKey="Gradient_DuskGlow"/>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="250" />
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Grid.Row="0" Margin="0,6,0,6">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                    <controls:CubeViewportControl  Width="72" Height="72" Scale="3.04" Axis="0,1,0.5" DurationSeconds="8" />
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,3,0,3">
                        <TextBlock x:Name="TitleText" Text="Congratulations!" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="HighScoreText" Text="New High Score!" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <controls:CubeViewportControl  Width="72" Height="72" Scale="3.04" Axis="0,1,0.5" DurationSeconds="8" />
                </StackPanel>
                <TextBlock x:Name="SubtitleText" Text="" FontSize="14" Foreground="LightGray" HorizontalAlignment="Center"/>
                <TextBlock x:Name="TimeText" Text="" FontSize="14" Foreground="LightGray" HorizontalAlignment="Center"/>
            </StackPanel>

            <Border Grid.Row="1" Grid.Column="0" Margin="8" Padding="6" Background="#22000000" CornerRadius="6" HorizontalAlignment="Center" Panel.ZIndex="1">
              <StackPanel>
                    <TextBlock Text="Recent Speed Runs" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,6" />
                    <controls:SpeedRunsView x:Name="SpeedRunsList" Height="230"/>
                </StackPanel>
            </Border>
          <Border Grid.Row="2" Grid.Column="0" Margin="8" Padding="6" Background="#22000000" CornerRadius="6" HorizontalAlignment="Center">
            <StackPanel>
              <Button x:Name="CloseButton" Foreground="White" Height="30"
                                Style="{StaticResource RoundedButtonStyle}"
                                Width="84" Padding="6,4" Margin="6,0,0,0" Click="CloseButton_Click">Close</Button>
            </StackPanel>
          </Border>
            <Canvas x:Name="RibbonCanvas" Grid.Row="0" IsHitTestVisible="False" Panel.ZIndex="2" />
            <Canvas x:Name="ConfettiCanvas" Grid.Row="1" ClipToBounds="True" IsHitTestVisible="False" Panel.ZIndex="0" />
        </Grid>
    </Border>
</UserControl>