<Window x:Class="LightsOutCube.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LightsOutCube"
        xmlns:models="clr-namespace:LightsOutCube.ViewModels"
        Title="About LightsOutCube"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FF111217"
        Foreground="White"
        FontFamily="Segoe UI"
        Loaded="Window_Loaded">
    <TabControl>
        <TabItem Header="About">
            <Border Padding="18" CornerRadius="6" Background="#DD111217">
                <!-- grid with icon + text -->
                <Grid Width="380">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="56" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Image x:Name="AppIconImage" Width="48" Height="48" Margin="0,0,12,0" VerticalAlignment="Top" Grid.Column="0" />

                    <StackPanel Grid.Column="1">
                        <TextBlock Foreground="White" Text="LightsOut Cube" FontSize="22" FontWeight="Bold" Margin="0,0,0,6"/>
                        <StackPanel  Orientation="Horizontal">
                            <TextBlock Foreground="White" Text="Version " FontSize="11" Opacity="0.9" Margin="0,0,0,12"/>
                            <TextBlock Foreground="White" Text="{Binding Version}" FontSize="11" Opacity="0.9" Margin="0,0,0,12"/>
                        </StackPanel>
                        <TextBlock Foreground="White" Text="A small WPF 3D Lights‑Out puzzle demo. Written by Gareth Richards." TextWrapping="Wrap" Margin="0,0,0,12"/>
                        <TextBlock Foreground="White" Text="Credits" FontWeight="SemiBold" Margin="0,8,0,4"/>
                        <TextBlock Foreground="White" Text="• Trackball code adapted from 3DTools sample" TextWrapping="Wrap" Margin="0,0,0,2"/>
                        <TextBlock Foreground="White" Text="• Gradient themes and animations by CoPilot" TextWrapping="Wrap" Margin="0,0,0,8"/>
                        <TextBlock Foreground="White" Text="Contact / Repository" FontWeight="SemiBold" Margin="0,8,0,4"/>
                        <TextBlock Foreground="White" TextWrapping="Wrap" Opacity="0.9" Margin="0,0,0,12">
                            <Hyperlink NavigateUri="http://github.com/GarethRichards/LightsOutCubeApp" RequestNavigate="Hyperlink_RequestNavigate">
                                http://github.com/GarethRichards/LightsOutCubeApp
                            </Hyperlink>
                        </TextBlock>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="CloseButton" Foreground="White" Height="30"
                                Style="{StaticResource RoundedButtonStyle}"
                                Width="84" Padding="6,4" Margin="6,0,0,0" Click="CloseButton_Click">Close</Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </TabItem>

        <!-- High scores tab -->
        <TabItem Header="High Scores">
            <Border Padding="12" CornerRadius="6" Background="#DD111217"
                    MinWidth="520" MinHeight="240" MaxHeight="550">
                <DockPanel LastChildFill="True">
                    <ListView x:Name="HighScoresListView" Background="#DD111217" Foreground="White" BorderThickness="0" ItemsSource="{Binding HighScores}" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.Resources>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="4,2"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </ListView.Resources>

                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Puzzle" Width="80" DisplayMemberBinding="{Binding Puzzle}" />
                                <GridViewColumn Header="Time" Width="120" DisplayMemberBinding="{Binding Time}" />
                                <GridViewColumn Header="Presses" Width="80" DisplayMemberBinding="{Binding PressCount}" />
                                <GridViewColumn Header="Perfect" Width="70" DisplayMemberBinding="{Binding PerfectText}" />
                                <GridViewColumn Header="When" Width="140" DisplayMemberBinding="{Binding Timestamp}" />
                            </GridView>
                        </ListView.View>

                        <ListView.Template>
                            <ControlTemplate TargetType="ListView">
                                <Border Background="{TemplateBinding Background}">
                                    <DockPanel>
                                        <GridViewHeaderRowPresenter DockPanel.Dock="Top"
                                                                    Columns="{Binding View.Columns, RelativeSource={RelativeSource TemplatedParent}}"
                                                                    ColumnHeaderContainerStyle="{Binding View.ColumnHeaderContainerStyle, RelativeSource={RelativeSource TemplatedParent}}"
                                                                    Margin="0,0,0,6"/>
                                        <ScrollViewer Focusable="false" VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </DockPanel>
                                </Border>
                            </ControlTemplate>
                        </ListView.Template>
                    </ListView>

                    <TextBlock x:Name="NoScoresText" Text="No high scores yet." Foreground="#BBBBBB" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HighScores.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DockPanel>
            </Border>
        </TabItem>

        <!-- Speed Runs tab -->
        <TabItem Header="Speed Runs">
            <Border Padding="12" CornerRadius="6" Background="#DD111217" 
                    MinWidth="520" MinHeight="240" MaxHeight="550">
                <DockPanel LastChildFill="True">
                    <!-- toolbar removed: SpeedRuns refresh occurs automatically -->

                    <!-- Header + TreeView for expandable per-run details -->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Column headings aligned with item template -->
                        <Grid Grid.Row="0" Margin="4,0,4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="140"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Solved" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="Total" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="2" Text="Last Puzzle" Foreground="White" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="3" Text="When" Foreground="White" FontWeight="SemiBold" />
                        </Grid>

                        <TreeView Grid.Row="1" x:Name="SpeedRunsListView" Background="#DD111217" Foreground="White" BorderThickness="0" ItemsSource="{Binding SpeedRuns}">
                        <TreeView.Resources>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Margin" Value="0,0,0,4"/>
                            </Style>

                            <!-- Top-level run template -->
                            <HierarchicalDataTemplate DataType="{x:Type models:SpeedRunEntryWrapper}" ItemsSource="{Binding PuzzleTimes}">
                                <Grid Margin="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="140"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding SolvedCount}" />
                                    <TextBlock Grid.Column="1" Text="{Binding TotalTime}" />
                                    <TextBlock Grid.Column="2" Text="{Binding LastPuzzle}" />
                                    <TextBlock Grid.Column="3" Text="{Binding Timestamp}" />
                                </Grid>
                            </HierarchicalDataTemplate>

                            <!-- Child item template for per-puzzle time details -->
                            <DataTemplate DataType="{x:Type models:PuzzleTimeEntry}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Data row: hidden when IsHeader == true -->
                                    <TextBlock Grid.Column="0" Margin="8,2,2,2" Text="{Binding Index}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="1" Margin="8,2,2,2" Text="{Binding Time}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="2" Margin="8,2,2,2" Text="{Binding PressCount}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="3" Margin="8,2,2,2" Text="{Binding PerfectText}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- Header row: visible only when IsHeader == true -->
                                    <TextBlock Grid.Column="0" Margin="8,2,2,2" Text="Puzzle" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="1" Margin="8,2,2,2" Text="Time" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="2" Margin="8,2,2,2" Text="Buttons" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="3" Margin="8,2,2,2" Text="Perfect" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHeader}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </TreeView.Resources>

                        <TreeView.Template>
                            <ControlTemplate TargetType="TreeView">
                                <Border Background="{TemplateBinding Background}">
                                    <ScrollViewer Focusable="false" VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </TreeView.Template>
                        </TreeView>
                    </Grid>

                    <TextBlock x:Name="NoSpeedRunsText" Text="No speed runs recorded." Foreground="#BBBBBB" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SpeedRuns.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DockPanel>
            </Border>
        </TabItem>
    </TabControl>
</Window>